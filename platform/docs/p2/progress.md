# P2 进度：通用问卷与本土入口

更新：2026-09-22

## 第一波（已合并，main `c7d79200`）

| 车道 | 内容 | 迁移 |
|---|---|---|
| 资源树 API | 建项目（仅租户管理员，建者得 `project_manager`）、建文件夹、按授权分页列出、改名、移动（源父与目标都需 `edit`，防环，按新位置继承授权） | V402 |
| 发布审批 | 提交 / 批准 / 驳回 / 撤回；批准绑定草稿版本，改稿即作废；允许有 `approve-publish` 者自批；发布只发被批准的版本 | V510 |
| 待核对后台核对 | 定时任务逐租户进入 RLS 作用域，同一 `requestId` 原样重发，指数退避，达上限转人工复核 | V520 |

规则见 ADR 0011。集成时补了审批 × 核对的交叉测试，并把测试连接池调小（多个缓存上下文与并行车道曾耗尽 PostgreSQL 连接）。

测试：平台 481 通过；P1 闸门端到端通过（项目已改为经 `POST /v1/projects` 创建）。

## 第一波遗留

- 资源节点不能删除（授权与问卷引用节点）；写接口没有幂等键；移动在极少数重试路径下可能死锁并返回 500。
- 审批人的 `approve-publish` 被收回后，已批准的申请仍有效；待审列表上限 200、无分页。
- 被标记人工复核的核对没有清除接口；核对批次按租户顺序消耗，大积压租户会拖慢后面的租户。
- 已发布问卷仍不能改版再发布（依赖网关支持）。

## 第二波（已合并，main `de93faba`）

| 车道 | 内容 | 迁移 / 契约 |
|---|---|---|
| WP-01 改版再发布与漂移 | 新版本即新引擎问卷；激活并绑定后原子切换公开路由，失败时旧版继续可访问；旧版以到期收口、保留答卷；漂移只标记不覆盖 | V530；ADR 0012；网关契约 v1.2 |
| WP-03 逻辑与计算 | 定义 v2 逻辑 DSL（条件、校验、计算值、文本引用）；类型检查、引用与循环检测；编译为引擎表达式并转义作者文本 | 契约 survey-logic-dsl-v1 |
| WP-06 答卷查询 | 按问卷分页查答卷（跨版本、游标无漏重）、字段字典、统计角色看不到明细、敏感字段脱敏；答案经网关按页读取 | ADR 0013；契约 response-read-v1 |
| WP-20.1 组织免登 | 企业微信 / 钉钉 / 飞书连接、免登、预授权或即时开通、离职与登出下一次请求即失权 | V105–V108；ADR 0014 |

集成时修复：
- 平台只接受定义 v1，会拒绝 v2 逻辑定义 → 放行 1、2；
- 项目 / 文件夹 id 在答卷与版本接口上返回空列表 → 404；
- 组织免登安全审查：新表补租户外键并改为带 `tenant_id` 的复合外键（外键检查绕过行级安全）；
  组织同步移除成员与手工撤权共用"最后一名所有者"判定（原副本忽略授权过期）；
- 测试环境：`mvn.sh` 不再重建共享 `platform-db`，端到端收尾不再停它；引擎端到端驱动跟随 `SURVEY_TEST_PREFIX`；测试入口脚本补可执行位。

测试：平台 635 通过；网关 379 通过；P1 端到端（含改版再发布）45 项、逻辑端到端 MySQL / PostgreSQL 各 30 项通过。

## 第二波遗留

- **`/v1/identity-bindings` 没有权限判定**（P1 起即如此）：租户内任意成员可创建或按主体查询外部身份绑定。组织免登复用这张表，未造成越权登录（新主体无成员资格），但必须收紧到"管理成员"——列为第三波第一项。
- 组织免登：事件回调、定时 / 分页同步、部门映射、撤销后恢复绑定、`start` 限流（网关层）、停用租户登录拒绝均未做；各开放平台部分接口字段待凭据到位后逐页核对。
- 改版切换时正在作答旧版的人在下一页被截断（无宽限期）；手工清除旧版到期会重新开放且漂移检查不报。
- 逻辑：清值依赖引擎 `deletenonvalues=1`（部署要求）；续答后的清值、计分与随机组（03.3）、日期类型未做。
- 答卷：手工停用再激活引擎问卷时的短暂编号窗口；06.2 导出作业未做。

## 第三波车道：WP-04.1 / 04.2 访问与作答规则（ADR 0016）

定义可带 `policy` 块（契约 survey-access-policy-v1，定义版本不变）。覆盖 R04-01（统一访问密码，
平台 PBKDF2 哈希、明文不落库）、R04-02 部分（邀请码＝参与者 token＋`access_mode=C`）、
R04-03 部分（token／设备／IP 组合限次）、R04-04（时区窗口、作答时长，服务端时钟）、
R04-05 部分（IP 规则；地区为可插拔数据源，无数据按 `regionUnknown` 拒绝或放行）。

| 层 | 内容 |
|---|---|
| 平台 | 草稿保存时 `access.password` → `passwordHash`，时间窗缺时区填 `Asia/Shanghai` |
| 网关 | `pubgw/policy/`：422 校验、本地时刻→UTC（夏令时缺口／重叠拒绝）、原生设置＋LSS `plugin_settings`、激活前回读摘要 |
| 插件 | `MjyRuntimePolicy`：`beforeSurveyPage` 闸门（时间窗→IP→地区→密码→限次→时长），fail closed；`policyStatus` 回读端点 |

测试：网关 448（新增 69）；插件 70（新增 46，MariaDB／PostgreSQL）；平台 644（新增 9）；
真引擎端到端 `run-access-policy.sh` 38 项，MariaDB／PostgreSQL 全过（含"改客户端时间不延期"、
"同一 token 换浏览器不能重新计时"、"解锁只对本 sid 有效"）。

遗留：地区数据集未交付；租户级时区；网关 `add_participants`
总是替换定义里的 token；同一身份两个会话同一瞬间交卷会落两份；密码错误次数只按会话计。


## 第三波其余车道（已合并）

| 车道 | 内容 | 迁移 / 文档 |
|---|---|---|
| 身份权限与组织同步 | `/v1/identity-bindings` 收紧：创建与按身份查询需租户级"管理成员"，按主体查询仅限本人或管理员；三家成员变更事件回调（验签、解密、官方测试向量）、分页与定时同步、撤销后恢复、非活动租户拒绝登录 | V109–V110；ADR 0014 增补 |
| WP-06.2 导出作业 | 建任务时固定快照、分批可续跑、下载时再次授权；CSV / XLSX＋字段字典＋附件清单；公式注入防护；不抓取任何 URL；30 万行 18.4 秒 | V600；ADR 0015 |
| WP-02 常用题型（第一批） | 47 项对照表；原生与原生加主题一批；手机号 / 邮编 / 身份证 / 统一社会信用代码服务端校验；30 种篡改提交双库验证 | `question-type-map.md` |

集成时修复：
- **CRITICAL：飞书事件回调是 CBC 填充预言机**——未签名请求跳过验签却仍用调用方给的 IV 解密，且各种失败返回不同状态与消息，匿名方不知密钥即可逐字节解密并伪造"成员离职"事件。现在任何解密之前先验签（握手也一样），所有取决于请求内容的拒绝统一为同一个 401 与同一段响应体（原因只进服务端日志）。发现于合并后的独立安全审查，修复前未推送。
- 共享 e2e 驱动 `Database` 的容器参数改为可选（此前我的改动弄坏了逻辑与答卷读取两个驱动）；答卷读取 runner 跟随容器前缀。
- 引擎测试栈切换 `TEST_DB` 时重启 web 容器：opcache 按时间戳隔几秒才重新校验，紧接着的请求会用上一种数据库的配置（顺序相关的偶发失败）。

测试：平台 741 通过；网关 535 通过；引擎端到端 P1（含改版再发布）、逻辑（双库）、答卷读取（双库）、题型（双库）、访问规则（双库）、网关服务全部通过。

遗留（第三波）：
- 事件回执不清理；部门映射、钉钉 Stream / 飞书长连接模式未做；钉钉离职事件字段需凭据到位后核对；停用租户已签发的令牌最长 10 分钟内仍有效。
- 导出：扩展表答案、附件打包、SAV / Word / PDF；多实例需共享存储或对象存储。
- 题型：伪造上传文件列表须以上传会话为准；5 类需新主题、16 类需插件扩展。
- 测试先行不完整：身份车道任务 2（事件回调、同步）与题型车道的 Java 字典测试是先有代码后补测试，未见过失败；导出车道先写测试但未先运行，事后以故意破坏实现确认测试能失败。事件回调已另经独立安全审查。

## 第三波遗留收尾（三项）

| 项 | 内容 | 验证 |
|---|---|---|
| 排序题名次读出为空 | 名次列是虚列，改由网关读端点按 `get_fieldmap` 认出排序题，把主列 JSON 摊到名次列（`pubgw/ranking.py`）；没排到的位置空串，整题不适用仍为 `null`。只请求名次列时自己补读主列。列结构按 `(实例, sid)` 缓存 | 网关单测；`run-question-types.sh` 场景 D2 双库逐名次核对；平台导出逐名次成列 |
| 匿名端点无限流 | `AnonymousRateLimitFilter` 挂在事件回调与免登两条安全链上，排在验签与读请求体之前；两层桶都按来源地址分，验签通过（2xx）退还令牌并改记「已验签」额度，攻击者锁不死真实回调；桶按最近使用淘汰；429 不说明原因 | 平台单测＋MockMvc（真实签名的回调在噪声被 429 时照常通过）；ADR 0014 增补三 |
| 平台不校验 `policyDigest` | 发布收尾时平台把自己发出去的策略按契约 §4 重新编译、算 SHA-256，与回执比对；缺摘要、不一致、算不出、以及"不需要插件却带摘要"都判发布失败（不登记路由、不写版本，引擎残留按孤儿记录） | 跨语言向量表 `digest-vectors.json`（网关与平台各读一次）；假网关的丢摘要／错摘要用例；`run-access-policy.sh` 每次发布都核对回执摘要（双库） |

仍未做：限流是进程内的，多实例部署时真实限额 ＝ 缺省值 × 实例数；反向代理后面须由入口层提供真实来源地址
（不认 `X-Forwarded-For`）。摘要重算是同一套规范化规则的第二份实现，分歧会误挡正常发布，靠向量表先红。

## 第四波（已合并，main `827d24c2`）

| 车道 | 内容 | 迁移 / 文档 |
|---|---|---|
| WP-05 投放触达 | 作答链接、二维码（纯 JDK 编码器）、短链、内嵌、签名渠道参数；批量投放任务（崩溃续跑不重发）、回执验签去重（重复回执不重复计费）、退订不可翻转；催答与新答卷通知 | V700–V799 |
| WP-18 通讯录 | 三类身份（员工／组织成员／答题联系人）在表结构上分离、显式可撤销关联、不做合并；名单导入去重（邮箱／手机／工号／外部身份）与坏行不中断；部门树、标签、部门级数据范围；联系人→参与者令牌映射 | V800–V803；ADR 0017 |
| WP-19 模板库与品牌 | 企业模板库（从问卷建模板剥离全部生产数据、版本、审核、复制、下架；租户模板与平台模板）；`zh-business` 主题去除上游标识与作答页全部外部请求；多语言；租户 CSS/JS 一律拒绝 | V540–V541；契约 survey-branding-v1 |
| WP-02 题型第二批 | 插件副表结构版本（第一批的前置阻塞）；六套真实主题：折叠文本、扫码录入、选项分组、矩阵分步、段落填空、自增表格、热区 | 契约 question-extension-tables-v1 |
| 遗留收尾 | 排序题按名次读回（网关统一解析，查询与导出共用）；免登与事件回调端点限流（验签通过者退还令牌并走独立额度，噪声打不掉真实回调）；平台校验网关返回的 `policyDigest`（旧版网关丢掉访问策略将无法发布），两端用同一张向量表钉住规范化 | — |

集成时修复：
- `PublishApprovalConcurrencyTest` 单独跑必然失败、整套跑却通过：`pg_stat_activity` 的后端状态快照按事务缓存，测试在持锁的长事务里轮询它，读到的永远是批准者连上来之前的快照；整套跑时又可能凭别的测试的等待会话假阳性通过。改为查 `pg_locks` 并钉死为本事务的等待者。生产端加锁正确（以"去掉行锁即变红"验证）。
- 引擎测试栈残留容器与 P1 端到端冲突（我自己上一轮手动运行未清理）；题型主题 runner 缺可执行位。

测试：平台 986 通过；网关 660 通过；引擎端到端 14 项（P1 含改版再发布、逻辑、答卷读取、题型、题型主题、访问规则、品牌主题、网关服务；涉及数据库的均双库）全部通过。

## 第四波遗留

- **按人判断是否已完成拿不到**：答卷投影没有参与者令牌，催答只认显式登记的完成记录；依赖 ADR 0016 待办（发布后从引擎读回邀请码）。
- 接真实短信／邮件通道还需：各通道实现、供应商幂等字段对接、回执格式与验签适配、短信模板与签名报备、真实计费价目表（现记条数非金额）。
- 通讯录：组织同步尚未写入 `org_member` 联系人；导入仅一次性 CSV/JSON（8 MiB／5 万行）；去重仅名单内。
- 模板库：无分页、平台模板无按租户定向、模板不是资源树节点（无单模板授权）；模板定义创建时未经网关校验。
- 品牌：翻译文本未过 v2 逻辑降级（翻译里的 `{}` 会被当表达式）；错误页仍带上游 generator 标记；未加 CSP；无租户素材上传。
- 题型：R02-04 多选需接管选项行模板；其余 13 类插件题型各自还需编辑器主题与列生成器；结构版本是声明而非推导（以绑定里的 `structureDigest` 兜底）。
- 限流按进程计：多实例时实际额度为 N 倍；真实按 IP 限流需入口层提供客户端地址（不信任 `X-Forwarded-For`）。

## 第五波（进行中）

环境：本机磁盘曾被写满（可用 115 MiB），Bash 完全不可用，第五波首批车道空手返回、未改一行。
清掉 24 个已合并的 agent worktree（约 10 GB，全部 dirty=0／ahead=0）后恢复。Docker 守护进程
随后仍因当时的 ENOSPC 楔住，且**菜单栏的 Restart 没生效**——所有 Docker 进程的启动时间仍是
卡死前那一刻，`_ping` 返回 000，楔住的后端等不到退出。强制结束整套进程再 `open -a Docker`，
20 秒就绪。守护进程被杀会留下 `survey-test-{db,web,pg}` 僵尸容器，`--fresh` 撞名失败，先删再跑。

| 车道 | 内容 | 状态 |
|---|---|---|
| 邀请码读回（ADR 0016 待办） | 发布回执带 `invitations[]`：引擎生成的 token 按定义顺序返回，平台可给每条一个不透明 `ref`；配不齐（行数对不上／缺 token／两条同码）发布失败并回滚 | 已完成并双库验证 |

按位置对应是唯一可靠的办法，不是图省事：引擎 `foreach ($aParticipantData as &$aParticipant)`
逐条按引用原地替换，返回与提交同序同长；而成功的条目被整条换成参与者表行属性，平台传的非列
字段已被 `array_intersect_key` 丢掉，姓名邮箱在开了字段加密的问卷上还是密文——端到端此前按
`firstname` 对应（直接查 `lime_tokens_<sid>`），在开加密的问卷上会断。

测试：网关 669 通过（660 → 669）；平台 986 通过、0 失败 0 错误（未动 Java，确认无回归）；
`run-access-policy.sh` 双库各 52 项 ok、0 FAIL。端到端不再直接查 `lime_tokens_<sid>`，改为从回执
取码并与参与者表逐条比对；后续 L2／D 场景用的就是回执里的码，能打开问卷、能触发按 token 限次，
所以这些码确实可用，不只是形状对。

**更正一处先前的说法**：这片并不解开催答与按人限次。那是两个独立缺口——
(a)「哪个码发给了谁」本片解决；(b)「一份答卷属于哪个码」没有解决。`ResponseProjection` 只有
答卷号、状态与时间戳，**没有 token**，`RespondentIdentityResolver` 这个接缝要的正是 (b)，主代码里
至今没有实现，网关 `responses.py` 里也完全没有 token 处理。下一片做 (b)：答卷读取暴露参与者令牌
（引擎在非匿名问卷的答卷表里有 `token` 列），(b) 通了 `RespondentIdentityResolver` 才能实现。

另一处前提：平台目前**根本不在定义里发 `participants`**（主代码里只有模板剥离逻辑提到这个键）。
所以「发布成功后按 `ref` 自动登记 `ContactParticipationService.link`」还缺上游一步——得有谁把联系人
名单物化成带 `ref` 的参与者，那属于投放车道，不在本片顺手改。

新增遗留：发布回执按 `requestId` **永久**存档，邀请码明文留在网关状态目录里；`ResultStore` 没有
保留期。改之前那个 SQLite 不含任何凭据。保留期是影响所有回执的横切改动，另开一片。

### 缺口 (b)：一份答卷属于哪个邀请码（已完成）

读端点新增可选 `includeRespondent`，每条答卷多回一个 `token`；不传时应答形状与契约本节之前逐字节
一致，也不会多问引擎一次问卷属性。只要身份时 `fields` 可传空数组，不必多请求一列无关作答。

匿名问卷永不给令牌。引擎只在非匿名时给答卷表建 `token` 列（`SurveyActivator.php:253`），但"列里
有值"不能当作可以发的依据：问卷可以先以非匿名激活、写满列与值，之后再改成匿名。所以按问卷**当前**
的 `anonymized` 判定，判定不出来就不给。端到端里这个状态是直接改库造的——引擎自己挡住了 RPC 这条路
（激活后 `set_survey_properties` 会剔除 `anonymized`，`remotecontrol_handle.php:506`），该状态只能
由直接改库／插件／运维造出来，而闸门的意义正是不信任列里有值。

`token` 不再能当普通列请求（400）。`fields` 的正则本来就匹配 `token`，直接写进去就绕过了匿名判定
——这是改动前真实存在的洞。

平台侧：`HttpResponseAnswerSource` 实现新的窄接口 `RespondentSource`；
`GatewayRespondentIdentityResolver` 据此实现 `RespondentIdentityResolver`，网关配置好时才注册
（`@Conditional`），催答对账自动生效。**令牌不落库**，每轮现读现用，平台库里不多一份可直接进入问卷
的凭据。

两处刻意钉死的行为：
- **读不到 ≠ 没答完**。网关不可达或拒绝时返回空映射，这一轮认不出任何人，不登记、不前进游标、
  下轮重来。若把读取失败当成"没有完成记录"，催答会朝一批其实已经答完的人发提醒。
- `RespondentIdentityResolver` 改为**按批**问。对账一页 200 份答卷、读端点一次收 500 个答卷号，
  逐条调用会把一次对账放大成两百次 HTTP。该接口主代码里本无实现，改动面封闭。

测试：网关 678 通过（676 → 678）；平台 996 通过、0 失败 0 错误（986 → 996）；
`run-response-read.sh` 双库均过，七项令牌断言全绿，其中两条是要害——读到的令牌正是发布回执里的
邀请码（(a) 与 (b) 在真实引擎上接上），以及改为匿名后引擎表里其实还存着令牌却返回 null。

遗留：`includeRespondent` 每次读取多一次 `get_survey_properties`，没有缓存。平台仍不在定义里发
`participants`，所以"联系人 → 邀请码"的上游（把名单物化成带 `ref` 的参与者）仍属投放车道待做。

## 第五波：导出格式扩展车道（WP-06.3，分支 `wp-06-export-formats`）

**SPSS SAV 导出（R06-03）已做完。** `ExportFormat` 加一项 `sav`，走的就是 ADR 0015 决定 5 预留的
扩展点：作业模型、分片格式、租约、崩溃恢复、下载再次授权一行没改，迁移 `V601` 只放开
`response_export_job.format` 的取值。产出是一个 ZIP：`responses.sav`（变量标签与值标签写在 SPSS
自带的字典里）＋ `variables.csv`（变量字典的可读副本，含消毒后的变量名 ↔ 原始列代码）＋
原样的 `fields.csv` / `attachments.csv`。取舍见 ADR 0015 增补一。

顺带做的两件小事：`ExportCellGuard` 把防公式注入从 `CsvExportWriter` 里提出来，CSV 与 SAV 共用一个
单元格出口（二进制格式也过闸——SPSS 能把数据集另存为 xlsx）；`ExportSheet` 多带表头行数与列的变量
描述，让写出方不必去猜哪几行是表头、也不必从"`code=text; …`"字符串里反解选项。

验证：平台全量 **1010 通过、0 失败 0 错误**（其中新增 `SavExportFormatTest` 9 项、
`ResponseExportSavTest` 5 项，`ResponseExportResumeTest` 的"崩溃恢复后逐字节相同"加跑了 `sav`）。
`target/export-sample.sav` 另用**第三方库 pyreadstat 1.2.7**（ReadStat C 库）读回核对：变量名、中文
变量标签、值标签、`F8.2`/`A16`/`A19`/`A255` 格式、UTF-8、记录数、SYSMIS→NaN、按 UTF-8 字符边界截到
253 字节、被加了单引号的公式文本，逐项与预期一致。网关与引擎未改动，不涉及端到端。

### 第五波遗留（本车道未做的三项，按原计划顺序）

- **扩展副表作答仍不进导出**（原计划第 1 项，未动）。卡在通道而不是导出：平台的字段字典只认引擎答卷
  列，副表作答按"题目代码 ＋ 代次 ＋ 行序 ＋ 列代码"存在插件的两张表里（契约
  `question-extension-tables-v1`），既没有引擎列名，也**没有任何读回来的路**——`MjyStructuredAnswerStore`
  的 `fetchRows()` / `fetchState()` 至今只有测试在调，插件没开过任何 RPC 或路由；网关只说 RemoteControl
  （纯标准库，没有数据库驱动），而 `remotecontrol_handle` 只给插件留了 `remoteControlLogin` 一个事件，
  加不进新的 RPC 方法。可行的形状是仿 `MjyRuntimePolicy::newDirectRequest`（网关已经在用它回读
  `policyStatus`）给 `MjyQuestionExtensions` 开一个读端点——但 `policyStatus` 只回份数与摘要，而副表作答
  是个人数据，**这条路要先有一套网关↔插件的共享密钥鉴权**，等于新开一条信任边界，应当先立 ADR。
  好消息是列字典不缺：`themeOptions.structureVersion` 与 `themeOptions.columns` 已经冻在平台的定义
  快照里（`pubgw/questions/themes.py` 的 `OptionSpec`），所以这一项**不需要**新迁移，也不需要把
  网关回执里的 `sideTable` 补进 `survey_question_binding`。
- **附件打包**（原计划第 2 项，未动）。引擎侧唯一的通道是 RemoteControl 的 `get_uploaded_files`
  （`remotecontrol_handle.php:3871`），它一次按**一份答卷**把全部文件 base64 塞进一个 JSON 应答里——
  要先解决"内存有界"（ADR 0015 的硬约束）与失败项重试（R06-07 的"失败项可重试"），还要新增网关端点与契约。
- **Word / PDF 导出**（原计划第 3 项，未动）。纯平台侧，不需要新通道，是三项里最独立的一项；
  `templateVersion` 的扩展位早就留好了。

为什么顺序没有照原计划走：第 1、2 项都要同时改 PHP 插件、Python 网关、Java 平台与契约，第 1 项还要先
立一条新的鉴权边界，双库端到端也得重跑；第 3 项（SAV）是 ADR 0015 自己指定的单组件扩展点。
按"做完一项算一项、不要全都做一半"，本轮只把能完整交付的那一项做完。
### WP-03.3／03.4：计分与双执行比对（已完成）

**计分（03.3）** 接在 03.1／03.2 之上，不另起炉灶：顶层 `scoring` 只被展开成本已有的 DSL——
按选项／子题给分展开成 `if(引用 == "代码", 分数, 0)`，数值题展开成 `coalesce(引用, 0) * 权重`，
总分是 `sum(…)`，分段是按上界嵌套的 `if`，每个分段的文案是一道条件为 `<CODE>B == "<分段代码>"`
的说明题——再走同一条 parser → types → graph → emit。所以「按分数分支／展示结果」就是普通的 v2
条件与文本引用，没有新机制。契约见 survey-logic-dsl-v1 第 6 节。

生成的题追加在一个**新的末尾题组**里。这不是随手放的：该组不带条件，分数就不会被别人的题组条件
连带清空；排在最后也让「只能引用前面的题」自动成立。代价是**前面的题不能引用分数**
（`E_EXPR_LATER_PAGE`），作者要按分数分支就得把那道题放进结果页或自己写条件。

转义按 03.2 的做法收口，但**加了一条更强的性质**：生成的表达式里一个字符的作者自由文本都没有——
只有数字、题目引用，以及平台已校验过字符集的选项／子题代码与分段代码。作者的自由文本（分数标题、
分段文案）全部落在题目的 text 上，由已有的模板路径转义。所以计分不需要一套新的转义规则。

**双执行比对（03.4）** 是本片的主交付。第 4 节的语义现在有两份独立实现：`pubgw/logic/evaluate.py`
直接在语法树上按契约求值，真引擎的 ExpressionManager 跑 `emit.py` 编出来的 ExpressionScript。
解释器**刻意只实现契约、不模仿 PHP 的类型杂耍**——引擎在契约之外的脾气正是比对要暴露的东西，
两边一起装傻就白比了。

分歧要能指出是哪一侧错，靠的是每条用例自带的、照契约**人工写下**的期望值：引擎报错判编译器；
分歧时谁合期望谁对；两边一致但都不合期望判 `both`；没有期望值判 `unknown` 并且**不许算通过**。

注入答案这条路有个要害：`_GetVarAttribute` 读 `.NAOK` 时，题目 `relevanceStatus` 的**缺省是 0**，
不显式置位的话每个引用都读成空串，比对会变成「两边都是空，恭喜全对」。`logic_parity.php` 每条用例
都把全部题目置为相关，再把该隐藏的置 0——这正好就是契约的「隐藏即空」。

结果（`run-publish-gateway-parity.sh`，双库各跑一遍）：**48 条表达式级用例（12 处表达式 × 4 份答案
向量），48 条都带期望值，mysql 与 pgsql 各 48/48 一致且合期望，零分歧。** 归责 platform／compiler／
both／unknown 均为 0。

「全对」本身证明不了比对还能报错，所以另有两道反向对照，都在脚本里常驻：
- **变异对照**：故意把编译产物改坏一次（`<=` 换 `>=`）、把平台的值谎报一次，比对必须抓住，
  并且判对是哪一侧——双库均通过；
- **场景级锚定**：再跑两次真实 HTTP 作答，引擎**真正存进答卷表**的分数与分段（9/MID、3/LOW）
  同平台算的逐一相等，证明注入会话那一套不是自说自话。

发布链路上改了一处：`Publisher.publish` 在校验之后、导入之前把计分展开一次，之后的回读校验、
字段映射、主题检查都按展开后的定义来。不改的话引擎里的分数题会被回读校验报成
`E_CODE_UNEXPECTED`（第一次真跑就是这么失败的）。

测试：网关 758 通过（678 → 758，新增 80 条：解释器 32、计分 34、比对 14），全部先写测试并亲眼见红
（`test_logic_evaluate`／`test_logic_scoring`／`test_logic_parity` 首次运行均 ModuleNotFoundError 或
失败）。未动 Java，未跑 Maven。端到端双库：`run-publish-gateway-parity.sh` 新增，mysql／pgsql 均过；
回归 `run-publish-gateway-logic.sh` mysql／pgsql 均过，`run-publish-gateway.sh` mysql 过。

遗留：
- 比对目前是**一次性快照**：答案向量与期望值都是手写的固定几份，没有按类型随机生成的对拍，
  也没有接进 CI 的定期跑。要覆盖到「所有题型 × 所有函数」还得再来一片。
- 解释器没有覆盖 `count()` 多参、双尺度 `[s]`、`O`／`K`／`Q` 等引用形态的比对用例——代码路径写了，
  但没有真引擎证据，样例问卷里没有这些题。
- 分段只支持按上界切分的一维分数；按题组分维度计分（多份 `scoring` 可以并存，但彼此独立）
  与加权平均还没有。
- `empty` 向量暴露一个语义角落并已双边确认：总分为空时 `STOTAL <= 4` 与 `<= 9` 都为假，会落进
  最后一段（HIGH）。实际不可达——总分是 `sum(…)`，永远是数字不会为空——所以没有改动，但若将来
  允许分段表达式引用可空的量，这里会咬人。

#### 复查修正（同片内）

复查报出、协调者独立复现的一条问题：`_reference_text` 把题目 UUID 插进 `q("<uuid>")` 时没有任何
字符集约束，`model.py` 的 `_text()` 对 `uuid` 只要求非空字符串。UUID 写成
`X") + 999, 0) * 1, sum(1` 就能从字符串字面量里逃出来。

**定性：不是提权。** 生成的文本随后由标准 v2 解析器重新解析，走同样的类型检查、引用检查与环检测，
而作者本来就能直接写 v2 DSL 表达式——注入者没有拿到他原本没有的能力。真正坏掉的是两件事：
(1) **正确性**：`_check_item` 校验的是原始引用，渲染出来的文本却可能指向另一道题；
(2) **不变式**：契约 §6 与 `scoring.py` 顶部都声称「生成的表达式里没有一处作者的自由文本」，
UUID 不受限时这句是假的，而且是潜伏危险——将来只要有一个插值点不再被重新解析，它就变成真注入。

修法取根治那条：在 `model.py` 的**解析期**给所有 `uuid`（问卷、题组、题目、子题、计分表）加字符集
`^[A-Za-z0-9][A-Za-z0-9_-]*$`、最长 128，一次关掉整类问题。取值范围同时容纳现存两种写法——JSON
fixture 里的标准 UUID 与 Python 单测里的 slug（`q-single`、`sq-m2`、`sq-{code}-{scale}`、`q-{code}`），
全仓 127 个 fixture UUID 与全部单测 UUID 无一被打死。各插值点（UUID、题目代码、子题／选项代码、
分段代码、分数代码）另加一层 `ScoringError` 断言作为纵深防御，让那句不变式成为本模块**自己**保证的，
而不是依赖上游没被改坏。契约新增 §8 写明这条约束与上面的定性。

同批处理的另外三项：
- **groupTitle 静默丢失**（MEDIUM）：多份计分表共用一个计分组，先前只取 `scoring[0].group_title`，
  其余静默丢弃。改为不一致即报 `E_SCORING_GROUP_TITLE`；一致时仍合并进同一组。补了「两个 score
  各有不同 groupTitle」与「两个 score 同 groupTitle 真的展开成一组」的用例——原有的
  `test_two_scores_may_not_share_a_code` 用的是冲突代码，根本走不到展开这一步。
- **`Infinity`/`NaN` 被接受**（MEDIUM）：`json.loads` 默认认这三个字面量，会被渲染成 `inf`/`nan`
  写进表达式再被引擎当标识符查。`_number()` 显式拒绝非有限浮点数。
- **`evaluate.py` 的 `ANY`/`CHOICE`/`TEXT` 死导入**（LOW）：删除。

四条都先写了能复现的失败用例并亲眼见红（UUID 三条 `DefinitionError not raised`、
`ScoringError` ImportError、`E_SCORING_GROUP_TITLE` 未报、三条非有限数值未拒）。

测试：网关 772 通过（758 → 772，新增 14：UUID 字符集 5、引用渲染 3、组标题 3、非有限数值 3）。
端到端**重跑了**双库比对（不是只靠推断）：对合法定义生成的表达式逐字未变、5 份问卷 fixture 全部仍能
解析，`run-publish-gateway-parity.sh` mysql 与 pgsql 仍各 48/48 一致、零分歧，两道反向对照与两条
场景级锚定全绿。
### WP-02 剩余插件题型（切片 02.4，进行中）

第四波遗留的题型部分。按 `question-type-map.md` 的顺序一类一类做完整（编辑器主题＋列生成器＋
四层测试＋真引擎双库端到端），**做完一类算一类**，不铺开做半成品。

| 需求 | 名称 | 做法 | 服务端闸门 |
|---|---|---|---|
| R02-04 | 选项分类的**多选**分支 | `mjy-grouped-options` 多一个 `multiplechoice` 视图目录，连 `rows/*.twig` 一起接管 | 引擎 `checkValidityAnswer` ＋ `max_answers`（数据形状与原生多选一致） |
| R02-11 | 循环评价 | `mjy-loop-rating`＋副表：一行一个评价对象、一列一个维度 | 对象列枚举＋唯一 ＋ 行数钉死成对象个数 |
| R02-17 | 图片 PK | `mjy-image-pk`＋副表：整题一行，一对一列 | 每列的取值恰好是这一对的两张图；另一列记录展示顺序 |
| R02-18 | 货架题 | `mjy-shelf`＋副表：一行一件商品 | 商品列枚举＋唯一，件数列整数带上下限 |

**为什么多选必须接管行模板**：单选靠 radio 的 `value` 就是选项代码，脚本照着 `value` 找行；
多选的 checkbox `value` 恒为 `Y`，代码只在字段名 `{SGQA}_S{sqid}` 里，而分组定义写的是子题代码。
所以行模板要把子题代码打成 `data-mjy-code`。引擎按
`<主题>/survey/questions/answer/<基础题型>/assets` 发布资产，一个基础题型一份，
两份脚本由测试钉死逐字节一致。

**一条通用能力换三类题型**：R02-11 给列定义加了两条**通用**约束——`type: "enum"` ＋ `options`
（取值集合由平台在发布时声明）与 `distinct: true`（同一列的非空取值不重复）。
R02-17 与 R02-18 因此**一行插件校验代码都没改**；四类下来插件侧只动过一次。
两条约束都进 `structureDigest`（副表契约 v1 §二补了「列字典里有什么」一节）。

另加一条注册表检查 `test_every_registered_type_has_its_own_view_folder`：
注册了某个题型却没建对应视图目录，在引擎上表现为静默降级，发布前根本看不出来。

测试：网关 735 通过（678 → 735，新增 57）；插件 PHPUnit 52 通过、151 断言（46 → 52），
MariaDB 10.11 与 PostgreSQL 16 各一遍；平台 Java 997 通过、0 失败 0 错误（996 → 997）；
`run-question-themes.sh` 双库**各 169 项 ok、0 FAIL**（第四波是 94）。
回归：`run-question-types.sh` 双库、`run-response-read.sh` 双库、`run-publish-gateway.sh` 均通过。

测试先行的执行情况（如实）：四类的网关、插件、Java 测试都是先写、先跑出红再实现，
每一处的红字样记在对应提交信息里。**一处例外**：Java 侧
`FieldDictionaryQuestionThemesTest.groupedMultipleChoiceKeepsOneColumnPerSubquestion`
（R02-04 那条）首次运行即绿，没有见过红——分组主题本来就不该改字段字典的形状，
这一条是把它钉住的回归钉子，不是驱动实现的测试。

遗留（本切片）：
- **剩 9 类插件题型未做**：R02-03（需字典服务与级联清值策略，不在本车道）、
  R02-20/21/25/26/32/39/41（都缺平台资产服务：媒体版本留存、授权播放、录音录像上传、
  图片合成、画布快照）、R02-22（文字点睛，原文版本＋偏移，可做，只是本轮没排上）、
  R02-46/47（试次反应时、按模型生成结构）。
- 图片地址（PK 的参赛图、货架图、热力图底图）仍是作者填的 URL，平台资产服务未接。
- 循环评价的评价对象是**静态声明**的；需求里的「对象动态」要等逻辑 DSL 的引用能力。
- 图片 PK 的 `_shown` 列不必填——关掉 JavaScript 直接填信封的那条路径给不出展示顺序，
  硬性必填等于把没有 JS 的人挡在外面；代价是那条路径下展示顺序不可追溯。
- 四类新题型的编辑器脚本只有「标记与 JS 真的出现在作答页上」这一层证据，
  没有浏览器里的交互测试（与第四波六套主题同一口径）。

## 第五波集成小结（main `67f47a8f`）

五条车道全部合入：邀请码回读与按人对账（`28e405ea`）、SAV 导出（`72c649c8`）、旧版恢复与文本
导入预览（`b2170e94`）、计分与双执行比对（`7df0cdcf`）、插件题型四类（`67f47a8f`）。

合并后基线：**平台 1052 通过、网关 829 通过，均 0 失败 0 错误**。数字可核对——平台
996＋14＋41＋1，网关 678＋94＋57，四条车道的新增彼此不干扰。

逐条合并、每条验证，**唯一的冲突是本文件**：四条车道都往波次汇总追加自己的章节，两次冲突都是
纯追加、内容不重叠。代码零冲突。经验：并行车道不要动共享汇总文档，由集成方统一写。

### 本轮新增的架构待办（需先立 ADR）

**网关↔插件的读取通道不存在**，这是导出车道撞上的硬阻塞，「扩展表答案导出」与「附件打包」都卡在
它上面：

- `MjyStructuredAnswerStore::fetchRows/fetchState` 除 store 内部一处自调用外**只有测试在调**，
  没有生产读路径（已独立核实）。
- 网关只会说 RemoteControl（纯标准库，无数据库驱动）。
- `remotecontrol_handle.php` 全文**只 dispatch 一个插件事件** `remoteControlLogin`（约 3971 行），
  加不进新的插件 RPC 方法（已独立核实）。

可行形状是仿 `MjyRuntimePolicy::policyStatus` 的 `newDirectRequest`，但那个只回计数与摘要，而扩展表
答案是**个人数据**，因此需要网关↔插件的共享密钥鉴权——一条新的信任边界，必须先立 ADR 再动手。
好消息：列字典不缺，`themeOptions.structureVersion` 与 `themeOptions.columns` 已冻在平台的定义快照
里，这一项不需要新迁移。

### 其余新增遗留

- **导出**：附件打包的唯一引擎通道 `get_uploaded_files` 一次把**整份答卷**的全部文件 base64 塞进一个
  JSON 应答，需先解决内存有界（ADR 0015 硬约束）与失败项重试；Word / PDF 未动，是三项里最独立的一项，
  `templateVersion` 扩展位已留好。
- **计分**：比对是**固定快照**（手写向量与期望值），没有类型驱动的随机差分测试，也没接 CI；
  `count()` 多参、双尺度 `[s]`、`O`/`K`/`Q` 引用形状缺真实引擎证据（样例问卷里没有这类题）；
  分段只支持一维上界切分；更早的题**引用不到分数**（`E_EXPR_LATER_PAGE`，位置换来的安全性）。
- **导入恢复**：只认一种文本写法，`一、`／`（1）` 等 Word 粘贴样式记坏行；选项与题目属性不能在文本里写；
  矩阵/双尺度不在本批。**恢复会顶掉未发布的草稿改动且不留备份**——这是「丢弃改动回到上一版」的正常
  语义，但前端必须有明确二次确认。
- **题型**：剩 9 类中 R02-03 卡平台字典服务（省市区树数千节点不能塞题目属性）、R02-20/21/25/26/32/39/41
  共 7 类卡平台资产服务；R02-22/46/47 无外部阻塞（下一波在做）。图片地址仍是作者填的 URL；循环评价的
  评价对象静态声明，「对象动态」要等逻辑 DSL 的引用能力；新主题的编辑器脚本只有「标记与 JS 真的出现在
  页面上」这一层证据，无浏览器交互测试（与第四波六套同口径）。
- **UUID 字符集**：现已在解析期限定 `^[A-Za-z0-9][A-Za-z0-9_-]*$`（最长 128）。此前无任何限制，可从
  `q("…")` 的字符串字面量逃逸。**不是提权**（生成文本会被标准 v2 解析器重新解析，走同样的类型/引用/
  环检查，且作者本就能直接写 v2 DSL）；真正的问题是 `_check_item` 校验的是原始引用而渲染文本可能指向
  另一道题，以及契约 §6 的不变式此前为假。
### WP-02 无阻塞的三类插件题型（切片 02.5）

剩下 9 类插件题型里，**只有 3 类没有外部阻塞**，本轮就做这 3 类，一类一条完整的链。

| 需求 | 名称 | 做法 | 服务端闸门 |
|---|---|---|---|
| R02-22 | 文字点睛 | `mjy-text-highlight`＋副表：一行一处标记，片段由平台声明 | 片段列枚举＋唯一，标记列枚举 |
| R02-46 | 心理实验 | `mjy-psych-trial`＋副表：一行一个试次 | 试次列枚举＋唯一、按键列枚举、反应时有界整数，行数钉死成试次个数 |
| R02-47 | 专业模型（KANO） | `mjy-model-kano`＋副表：一行一个功能点，正反两问 | 功能点列枚举＋唯一，两问枚举到**模型固定**的五点量表，行数钉死成功能点个数 |

**三类都没动插件一行校验代码**，用的全是切片 02.4 的通用列约束。三类下来只提取了
**一条**新的通用能力：`theme_columns.bounded_rows_issues`——「行数下限不得超过唯一列的
取值个数」在货架题与文字点睛逐字相同，这才是第二次出现。

三处「不让作答者自称结论」的同型设计，值得单独记一笔：

- 文字点睛：片段代码是 `s<起点>_<长度>_<该段原文的指纹>`，所以「偏移对、原文换了一版」
  也进不来——需求里的「中文标记偏移和原文版本一致」在服务端有了依据；
- 心理实验：列定义里**根本没有放对错的列**，正确率由平台按 `trials[].correct` 推导；
- KANO：列定义里**根本没有放分类的列**，分类由平台按正反两问算。

三类各有一条篡改用例专打这一点（提交旧指纹的片段、夹带 `correct`、夹带 `category`），
全部作为未知列或越界取值被拒收。

另加一份跨语言的注册表检查 `tests/test_plugin_registry_parity.py`：网关的
`STRUCTURED_THEMES` 与插件 `MjyThemedQuestionMap`、平台 `QuestionTypeColumns` 按源码文本对一遍，
`MANAGED_ATTRIBUTES` 与插件的属性声明也对一遍。三处漂移都要装好引擎才看得见
（无人校验 / 导出标签错 / JSON 被 XSS 净化改写），这份用例不需要 PHP 与 JVM，跟着网关单测跑。

文件规模按批次提前拆开，没有等到撑爆再返工：`theme_specs.py`（注册表＋展示型主题）、
`theme_structured.py`（02.3–02.4 的四类）、`theme_research.py`（本批三类），各 200–340 行。

测试：网关 789 通过（735 → 789，新增 54）；插件 PHPUnit 52 通过、164 断言（151 → 164），
MariaDB 10.11 与 PostgreSQL 16 各一遍；Java `FieldDictionaryQuestionThemesTest` 4 通过；
`run-question-themes.sh` 双库**各 248 项 ok、0 FAIL**（切片 02.4 是 169），
篡改场景 D 共 55 条（本批新增 22 条）。

测试先行的执行情况（如实）：三类的网关用例、注册表用例与 Java 字段字典用例都是先写、
先跑出红再实现，红字样记在各自的提交信息里（网关三次分别是 20/8、15/9、9/8 条失败与错误；
Java 三次都是「标题少了 `[结构化作答]`」）。**一处例外**：插件 PHPUnit 里
`mjy_highlight_*` / `mjy_psych_trials` / `mjy_model_*` 的 `xssfilter` 断言是**属性先注册、
再加进断言列表**的，首跑即绿，没见过红——它是回归钉子，不是驱动实现的测试；
同一类漂移由上面那份 parity 用例见过红。

遗留（本切片）：
- **R02-46 的设备元数据未做**。需求里那一项是一次作答一份（浏览器、屏幕、计时精度），
  不是一个试次一份，塞不进这张一行一试次的副表；硬塞成每行重复一遍既冗余又同样
  由客户端提供、同样不可信。归运行时 R08-14。
- **R02-47 只有 KANO 一个模型**。MaxDiff、联合分析、PSM、Gabor-Granger、AHP、TURF、BPTO
  各自还要设计生成器；其中 MaxDiff 的「最好 ≠ 最差」是**跨列**约束，现有通用列约束
  表达不了，要么给插件加一条跨列规则、要么换一种列布局，不顺手做。
  分类与 Better／Worse 系数的计算属于统计金标准（WP-08），不在本车道。
- **R02-22 的一个已知缺口**：改动最后一个片段之后的文字不会改变任何片段代码，
  因此不强制换结构版本。那种改动不会让已有的标记指错地方（每条标记指的字与内容都没变），
  为它硬性要求换版等于制造一条没人遵守的规矩。
- 心理实验的 `rt` 由浏览器 `performance.now()` 测量，主题在刺激上屏那一帧起表，
  但**不承诺等同实验室硬件**；平台只保证这个毫秒数被原样、有界地记下来。
- 剩下 6 类插件题型仍未做，全部有外部阻塞：R02-03 需平台字典服务与级联清值策略；
  R02-20/21/25/26/32/39/41 需平台资产服务。本轮没有为它们做任何占位实现。
- 三套新主题的编辑器脚本仍然只有「标记与 JS 真的出现在作答页上」这一层证据，
  没有浏览器里的交互测试（与前两波同一口径）。

## 第六波（已合并，main `5e602b57`）

| 车道 | 内容 | 迁移 / 文档 |
|---|---|---|
| 网关↔插件鉴权通道 | 通道密钥自实例密钥再派生一层（零新增引擎配置，单向：网关被攻破能读副表作答但伪造不了引擎事件）；签名覆盖整个规范化查询；扩展表作答经 `extensionAnswers` 进入按页作答读取 | ADR 0018；契约 `plugin-channel-v1` |
| 插件题型第四批 | R02-22 文字点睛、R02-46 心理实验、R02-47 专业模型 KANO | 契约 `question-extension-tables-v1` |
| 联系人物化成参与者 | 受众存条件非名册；发布时物化成 `participants[]`（只带 `ref`＝联系人 id）；发布成功后在同一事务里把回执的 `invitations[]` 登记成联系人↔令牌映射 | V804；ADR 0017 决定 8 与诚实清单 #6/#7 |

测试：平台 1082 通过、网关 941 通过，均 0 失败 0 错误；插件 PHPUnit 双库各 110 测试 / 248 断言；
`run-access-policy.sh` 双库各 59 项、`run-question-themes.sh` 双库各 248 项、`run-response-read.sh`
与 `run-question-slice.sh` 双库均过。

### 三处非显然的判断（留档，免得日后被当成随手写的）

- **统一拒绝的边界划在验签处**，而不是一刀切。验签及其之前一律返回逐字节相同的 401（飞书填充
  预言机那次的教训）；验签**之后**才允许区分错误——没有密钥根本到不了那里，不构成预言机，而
  一刀切会白白丢掉全部可诊断性。`sid` 不存在／代次不匹配返回 **200 空**而非错误，不泄漏问卷是否存在。
- **验签在前、限流在后**，与 `AnonymousRateLimitFilter` 的顺序**故意相反**：这里的 HMAC 是纯进程内
  计算，限流在前反而会让匿名请求先写一次数据库计数器。
- **`extensionAnswers` 单独成段**，不并进扁平的 `values`：副表是行×列、每份答卷行数不同，压平会
  截断答案或让列集漂移。

### 独立审查抓到的问题（4 HIGH ＋ 1 CRITICAL，全部属实并已修）

这些都不是单元测试能发现的类别，靠的是有人专门去读改动面并质疑不变式。**其中两处，车道自己原有
的测试正好把错误行为钉住了**——只看绿灯会四个全漏。

- **CRITICAL**：`ensureSchema()` 在 `handle()` 之前跑，而 Yii 的 `getTable($name, true)` 必然绕过缓存
  落库——每个匿名请求都强制一次实时 schema 查询，**直接证伪了 ADR 0018「验签前零 IO」这个核心论断**。
  原有测试只覆盖端点、没覆盖插件接线。修法为惰性建表，并以「把那行加回去看测试转红」做变异验证。
- **HIGH**：受众读取路径缺按问卷授权（IDOR）。`set()`／`clear()` 都调 `requireSurvey`，`find()`／
  `recipients()` 只调租户级通讯录权限，而 `set()` 的注释自己写着「问卷也是」——是遗漏不是设计。
- **HIGH**：行锁内的 N+1。每个邀请码调一次 `liveVersion`，后者反序列化**每个已发布版本的完整 JSON
  定义**，而那份定义里正好嵌着这批 `participants[]`；2000 人受众即数千次往返，全在持着问卷行锁的
  结算事务里。改为从 `recordPublished` 把版本传下去。
- **HIGH**：认不出的 `ref` 这类**重试必然同样失败**的情形被判成 `pending_reconciliation`，会永远卡住
  （对账重放的是同一份存档回执）。这与同类中 `requireIssuedInvitations` 的推理自相矛盾。现归
  `publish_failed`；原有测试把错误行为钉住了，断言随之修正。
- **HIGH（计分车道，第五波）**：`uuid` 无字符集限制，可从 `q("…")` 的字符串字面量逃逸。已在解析期
  限定。**不是提权**（生成文本会被标准 v2 解析器重新解析，走同样的类型/引用/环检查，作者本就能直接
  写 v2 DSL）；真正的问题是 `_check_item` 校验的是原始引用而渲染文本可能指向另一道题。

### 第六波新增遗留

- ~~**扩展表答案已到平台，但尚未进 CSV/XLSX/SAV**~~ → 第七波切片 06.4 做完了，见 ADR 0015 增补二。
  不定行数的落法：**单列一张长表 `extensions`，一个单元格一行**，固定十列
  （`version sid responseid generation question structureversion isvalid rowindex column value`）。
  否决了「宽表加序号后缀」（需要一个冻结表头时并不知道的行数上限，硬压要么截断要么让列集漂移——
  与网关侧 `extensionAnswers` 单列一段是同一条理由），也否决了「每行一条记录、列代码横着展开」
  （表头要一份冻结的列字典，而八个 C 档主题里只有 `mjy-repeating-table` 的列就是定义快照里的
  `themeOptions.columns`，另外七个由网关 `side_columns` 各自推出来，平台再写一遍就是契约警告过的那种重复）。
  代价：文件行数 ＝ 单元格数，分析前要透视一次。列字典（列标签、类型、取值集合）仍不进导出。
- **Word 导出做完了，PDF 没做**（切片 06.4，ADR 0015 增补三）。逐份答卷成文：一份答卷一页，
  「题目｜作答」成对，副表作答在文档里**透视回真表格**（文档格式一次只持有一份答卷，透视得起——
  正好补上长表那笔代价）。分片随之改成按答卷交替存放（`R` 后面紧跟它的 `A` 与 `E`），两种读法都仍是
  流式的，既有格式的输出字节不变。PDF 卡在**中文字体**：冻结工作表 P-10 已确认「自带并嵌入」、
  否决了靠阅读器字体包的备选，而仓库里没有可嵌的中文字体（`assets/fonts/` 里 CJK 的几个是占位说明）。
  要往下走先要有人决定用哪款字体、什么许可、放哪儿——不是导出车道能单方面定的。
  自定义 Word 模板（R06-06 后半句）也没做：`templateVersion` 仍只接受空或 `default`。
- **附件打包**仍卡在 `get_uploaded_files`：它一次把**整份答卷**的全部文件 base64 塞进一个 JSON 应答，
  需先解决内存有界（ADR 0015 硬约束）与失败项重试。
- **通道是 GET**：`application/config/internal.php:153` 只对 `rest`／`admin/remotecontrol`／
  `plugins/unsecure` 免 CSRF，POST 到 `plugins/direct` 在事件分发前就被 Yii 拒掉。代价是
  sid／代次／答卷号会进访问日志（答案值只在应答体里）。契约禁止在这条通道上放写操作而不重开此决定。
- **邀请码残留**：`contact_participation` 已 REVOKE DELETE，`revoke` 只置 `revoked_at`，所以失效令牌
  的文本仍留在行里。旧引擎问卷已关闭故这些码打不开任何东西，但「过期凭据不落盘」需要运维清理策略。
- **题型**：R02-46 的设备元数据是每份答卷而非每个试次，不属于这张副表（归 R08-14 运行时）；
  R02-47 只做了 KANO 一个模型，MaxDiff 的「最好≠最差」是现有通用约束表达不了的跨列约束；
  R02-22 在最后一个片段之后改文本不会改变任何东西（也不会误导既有标记）。

## 平台字典车道（分支 `wp-02-dictionary`，基线 main `acafb55a`）

| 内容 | 迁移 / 文档 |
|---|---|
| 平台层级字典：字典 / 版本 / 节点三张控制平面表，版本化（发布即冻结）、按父节点分页、关键字搜索、服务端路径判定；发布时把引用到的字典固化成当前版本并把节点快照写进定义（新接缝 `SurveyDictionarySource`，与 ADR 0016 的 `participants` 物化逐条对应） | V900；ADR 0019；契约 `platform-dictionary-v1` |
| R02-03 多级下拉：新主题 `mjy-cascading-select`，新列类型 `dict`（取值集合是字典某一版的第 N 层，不在题目属性里），快照随 `.lss` 的 `plugin_settings` 到引擎并物化成两张索引表，作答页按层分页取，服务端逐级核对整条路径 | 契约 `question-extension-tables-v1` 补 `dict` 列 |
| 离线地区数据源收掉逐行扫描：预排序定宽索引＋二分（ADR 0016 缺口） | ADR 0016 缺口清单 |

测试：平台 1083 → 1132（+49，恰是新增的 4 个测试类：13/21/7/8），0 失败 0 错误；
网关 941 → 967（+26，`test_question_cascading.py`）；插件 PHPUnit 双库各 110 → 147
（+37：字典表 13、路径判定 13、作答页端点 11），337 断言；
`run-tests.sh -c platform/phpunit-runtime-policy.xml` 双库各 70 → 81（+11 地区索引），392 断言；
`run-question-themes.sh` 双库各 248 → 278 项（+30，两道多级下拉题分别引用同一本字典的两版）。

### 只有真引擎端到端才抓得到的一处

`DbStorage::getGeneric()` **已经替调用方 json_decode 过** `plugin_settings` 的值
（`application/libraries/PluginManager/Storage/DbStorage.php:60`），而插件原先只认字符串。
后果是字典一本都装不上、每一条合法路径都被判成非法——整道题谁也交不了。
三个单元层（平台 / 网关 / 插件）全绿，端到端第一次跑就红。这条与前几波「只看绿灯会漏」的
教训同一类：把每一段都测到，不等于把段与段之间的接缝测到。

### 三处值得留档的判断

- **版本绑定沿用「发布时物化」而不是新造一套。** 字典更新后老问卷必须继续按发布时那一版校验，
  这与 ADR 0016 的受众物化、ADR 0012 的「新版本即新引擎问卷」是同一类问题，因此用同一套做法：
  草稿里只有「引用哪本字典」算数，版本与摘要由发布时固化进不可变的定义快照。
- **快照走 `plugin_settings` 而不是新开一条安装通道。** 访问策略已经这么走，`value` 是
  `mediumtext`，三千多个节点约 120 KB 装得下，**不新开信任边界**。代价是节点数被定义快照的
  1 MiB 上限框在 8000——到乡镇街道那一级就不够了，届时才需要重开 ADR 0018 决定 4。
- **作答页那条字典端点刻意不接 `MjyChannelRateLimit`。** 那张计数表是落库的，接在一条无签名的
  端点上正好造出 ADR 0018 决定 6 警告过的放大面。改为「形状检查全部在任何数据库访问之前」，
  并如实记下「没有按 IP 的限流」这条缺口。

### 本车道的新增遗留

- 作答页脚本的**交互**没有浏览器里的测试：端到端证明了标记与 JS 出现在页面上、服务端对
  手写信封的判定正确，但「点了省之后真的取到了市」只有端点自己的 PHPUnit 覆盖。
- 引擎侧的旧版字典快照没有回收策略；作答页端点没有按 IP 的限流；
  作者不能钉死某个历史版本；搜索是 `LIKE '%…%'`；一道题只渲染一组层级链。
- 三端（平台 / 网关 / 插件）的节点数、层数、页大小上限靠注释互指，
  **没有像 `STRUCTURED_THEMES` 那样的自动一致性检查**。
